#!/usr/bin/env bash
# Install nginx and add custom header X-Served-By

apt-get update -y
apt-get install -y nginx

sed -i '/server_name _;/a add_header X-Served-By $hostname;' /etc/nginx/sites-available/default

service nginx restart