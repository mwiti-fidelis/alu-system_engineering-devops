#!/usr/bin/env bash
# Script to configure Nginx with custom X-Served-By HTTP response header

# Update package lists
sudo apt-get update

# Install Nginx if not already installed
sudo apt-get install -y nginx

# Get the current hostname
HOSTNAME=$(hostname)

# Remove any existing X-Served-By header lines to avoid duplicates
sudo sed -i '/add_header X-Served-By/d' /etc/nginx/sites-available/default

# Add the custom header after the server_name directive
sudo sed -i '/server_name _;/a\    add_header X-Served-By '"$HOSTNAME"' always;' /etc/nginx/sites-available/default

# Test Nginx configuration
sudo nginx -t

# Reload Nginx to apply changes
sudo service nginx reload

# Verify the header is working
curl -sI http://localhost | grep X-Served-By